name: workforce_ingestion
version: "1.0.0"
config-version: 2
profile: workforce

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]

clean-targets:
  - target
  - dbt_packages

models:
  workforce_ingestion:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: analytics
      +materialized: table
    reports:
      +schema: analytics
      +materialized: table
